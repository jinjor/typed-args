{
  "name": "@jinjor/typed-args",
  "version": "0.1.0",
  "main": "dist/src/index.js",
  "license": "MIT",
  "repository": "git://github.com/jinjor/typed-args.git",
  "scripts": {
    "prettier": "prettier src/**/*.ts test/**/*.ts",
    "format": "npm run prettier -- --write",
    "test": "tsc && npm run format -- --check && node dist/test/test",
    "preversion": "npm test",
    "prepublishOnly": "npm test"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.1",
    "@types/node": "^14.14.14",
    "prettier": "^2.2.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "minimist": "^1.2.5"
  }
}
